[project]
name = "llm-steganography"
version = "0.1.4"
description = "Encode→decode linguistic steganography: hide a bit in a TinyStories rewrite, then recover it."
tags = ["steganography", "tinystories", "multi-turn", "train", "eval"]
requires-python = ">=3.11"
dependencies = ["verifiers", "datasets"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["llm_steganography"]
